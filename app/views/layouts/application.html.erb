<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= content_for(:title) || "New Era Lissoise" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%# PWA manifest for mobile installation %>
  <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

  <link rel="icon" href="<%= asset_path 'New era lissoise.png' %>" type="image/png">
  <link rel="apple-touch-icon" href="<%= asset_path 'New era lissoise.png' %>">

  <%= stylesheet_link_tag 'app', media: 'all', 'data-turbo-track': 'reload' %>
  <%= javascript_importmap_tags %>
</head>
<body>
<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      

      <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
        <ul class="navbar-nav ms-auto d-flex">
          <% if user_signed_in? %>
            <li class="nav-item">
              <span class="navbar-text text-white mx-2">Welcome back <%= current_user.first_name %> 👋</span>
            </li>
            <li class="nav-item">
              <%= link_to "Create an Event", new_event_path, class: "btn btn-primary mx-2" %>
            </li>
            <li class="nav-item">
              <%= link_to "Members", members_path, class: "btn btn-primary mx-2" %>
            </li>
            <li class="nav-item">
              <%= button_to "Log Out", logout_path, method: :delete, class: "btn btn-danger mx-2" %>
            </li>
          <% else %>
            <li class="nav-item">
              <span class="navbar-text text-white mx-2">Welcome to New Era Lissoise! Log in to join an event</span>
            </li>
            <li class="nav-item">
              <%= link_to "Register", signup_path, class: "btn btn-primary mx-2" %>
            </li>
            <li class="nav-item">
              <%= link_to "Login", login_path, class: "btn btn-primary mx-2" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</header>

  <!-- Main Content -->
  <div class="container mt-4">
    <%= yield %>
  </div>

    <!-- Footer -->
  <footer class="footer bg-dark text-white py-3">
  <div class="container">
    <div class="btn-container">
      <%= link_to "Contact us", contact_path, class: "btn btn-primary" %>
      <a href="https://www.facebook.com" target="_blank" class="btn btn-primary">
        <img src="/assets/facebook-logo.png" alt="Facebook Logo" class="facebook-logo me-2">
        Join us on Facebook
      </a>
      <%= link_to "Privacy Policy", privacy_policy_path, class: "btn btn-primary" %>
    </div>
  </div>
  <p class="text-center mt-3 mb-0">© <%= Time.now.year %> New Era Lissoise - All rights reserved</p>
</footer>
</body>
</html>
